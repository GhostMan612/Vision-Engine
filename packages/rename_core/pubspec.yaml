name: rename_core
description: "Vision Engine pure-Dart rename engine: faithful port of rename_camera_prefixes.py plan semantics."
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  test: ^1.26.0
  lints: ^6.1.0
