# vision-core — pure JVM domain contracts for Vision Engine native.

Future home (MP2+) of the ported `metadata_core` / `media_library` /
rename-plan semantics. MP1 establishes the namespace, the JVM target,
and the executable Android-independence boundary
(`ModuleBoundaryTest`) — nothing else, by design.
