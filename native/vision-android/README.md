# vision-android — Android platform implementation layer.

Future home (MP3+) of SAF access, ExifInterface/MediaMetadataRetriever
extraction, and bitmap thumbnail work. MP1 establishes the package
boundary (`com.visionengine.android`), the `:vision-core` dependency,
and the compile gate — no platform code yet, by design. No UI here, ever.
